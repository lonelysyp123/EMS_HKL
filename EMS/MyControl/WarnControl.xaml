<UserControl x:Name="userControl" x:Class="EMS.MyControl.WarnControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EMS.MyControl"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="100">
    <Grid>
        <!--BCMU三种告警：（一级告警为黄色，二级告警为橙色，三级告警为红色）-->
        <Border BorderThickness="1"
                CornerRadius="5"
                Padding="5"
                Width="130"
                Height="30"
                Background="{Binding BorderBackground, RelativeSource={RelativeSource AncestorType=UserControl}}"
                BorderBrush="{Binding BorderColor, RelativeSource={RelativeSource AncestorType=UserControl}}">
            <TextBlock HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Text="{Binding AlertText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       Foreground="{Binding TextColor, RelativeSource={RelativeSource AncestorType=UserControl}}" />
        </Border>
    </Grid>
</UserControl>
